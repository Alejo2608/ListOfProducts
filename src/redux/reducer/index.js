
const initialState={
    info:[]
}

export default function rootReducer(state=initialState,action){
    switch (action.payload) {
        case value:
            
            break;
    
        default:
            break;
    }
}